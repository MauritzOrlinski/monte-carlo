#ifndef GEOMETRY_H
#define GEOMETRY_H

#include "GL/glut.h"
#include <stdint.h>
#include <sys/types.h>

void draw_circle(float, float, float);
float dist(float, float, float, float);
#endif // !GEOMETRY_H
